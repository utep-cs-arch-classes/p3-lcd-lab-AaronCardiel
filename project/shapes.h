// Draws sprites for each state
void drawSaxophone(int col, int row);
void drawMoon(int col, int row);
void drawBunny(int col, int row);
void drawHand(int col, int row);

// Clears area of sprite
void clearShape();

// Helps drawing moon sprite
void drawLines(int x_coord, int y_coord, int x_point, int y_point, int color);
void drawHorizontalLine(int x_start, int x_end, int y, int colorBGR);
